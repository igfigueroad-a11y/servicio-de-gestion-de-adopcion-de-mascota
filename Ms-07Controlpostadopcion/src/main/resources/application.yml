spring:
  application:
    name: mscontrolpostadopcion

  datasource:
    url: jdbc:mysql://localhost:3306/visitas_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087