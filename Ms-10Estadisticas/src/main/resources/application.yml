spring:
  application:
    name: estadisticas

  datasource:
    url: jdbc:mysql://localhost:3306/estadisticas_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090