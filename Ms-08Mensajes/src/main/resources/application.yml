spring:
  application:
    name: mensajes

  datasource:
    url: jdbc:mysql://localhost:3306/avisos_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8088