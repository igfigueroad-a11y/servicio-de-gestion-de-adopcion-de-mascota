spring:
  application:
    name:  Ms-06Procesoadopcion

  datasource:
    url: jdbc:mysql://localhost:3306/adopciones_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8086
  

springdoc:
  api-docs:
    enabled: true
    version: openapi_3_0
  swagger-ui:
    enabled: true