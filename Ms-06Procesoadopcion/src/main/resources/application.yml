spring:
  application:
    name: msprocesoadopcion

  datasource:
    url: jdbc:mysql://localhost:3306/adopciones_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8086